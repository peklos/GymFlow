# 🚀 Полное руководство по деплою GymFlow

Это руководство покажет, как задеплоить полный стек GymFlow:
- **Backend** на Render с базой данных Neon (PostgreSQL)
- **Frontend** на Netlify

## 📋 Предварительные требования

- Аккаунт на [GitHub](https://github.com) (код должен быть в репозитории)
- Аккаунт на [Neon](https://neon.tech) для PostgreSQL
- Аккаунт на [Render](https://render.com) для backend
- Аккаунт на [Netlify](https://www.netlify.com) для frontend

## 🗄️ Шаг 1: Создание базы данных на Neon

### 1.1 Регистрация и создание проекта

1. Перейти на https://neon.tech
2. Зарегистрироваться / войти через GitHub
3. Нажать **"Create a project"**
4. Заполнить форму:
   - **Project name**: `gymflow-db` (или любое другое)
   - **Region**: `AWS / Europe (Frankfurt)` (или ближайший к вам)
   - **PostgreSQL version**: `16` (последняя версия)
5. Нажать **"Create project"**

### 1.2 Получение строки подключения

1. После создания проекта открыть **"Dashboard"**
2. Найти секцию **"Connection string"**
3. Выбрать формат **"Parameters only"** или **"Connection string"**
4. Скопировать строку подключения, она выглядит так:

```
postgresql://username:password@ep-xxx-xxx-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

5. **ВАЖНО**: Сохраните эту строку - она понадобится для Render!

### 1.3 Настройка базы данных (опционально)

База данных создастся автоматически при первом запуске приложения, но можно проверить:

1. Перейти в **"SQL Editor"** в Neon
2. Выполнить тестовый запрос:

```sql
SELECT version();
```

## 🖥️ Шаг 2: Деплой Backend на Render

### 2.1 Подключение GitHub репозитория

1. Перейти на https://render.com
2. Войти через GitHub
3. Нажать **"New +"** → **"Web Service"**
4. Выбрать ваш GitHub репозиторий `GymFlow`
5. Нажать **"Connect"**

### 2.2 Настройка Web Service

Заполнить форму создания сервиса:

| Поле | Значение |
|------|----------|
| **Name** | `gymflow-api` (или любое уникальное имя) |
| **Region** | `Frankfurt (EU Central)` (или ближайший) |
| **Branch** | `claude/backend-setup-start-011CUpeA3Yjvf432BVKuq5vi` (ваша ветка) |
| **Root Directory** | `back` ⚠️ ВАЖНО! |
| **Runtime** | `Python 3` |
| **Build Command** | `./build.sh` |
| **Start Command** | `./start.sh` |
| **Instance Type** | `Free` (для начала) |

### 2.3 Добавление переменных окружения

В секции **Environment Variables** добавить:

| Key | Value |
|-----|-------|
| `DATABASE_URL` | Ваша строка подключения из Neon (из шага 1.2) |
| `PYTHON_VERSION` | `3.11.0` |

**Пример DATABASE_URL**:
```
postgresql://username:password@ep-xxx-xxx-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

### 2.4 Запуск деплоя

1. Нажать **"Create Web Service"**
2. Render начнёт сборку и деплой (займёт 3-5 минут)
3. Следить за логами в реальном времени
4. После успешного деплоя появится URL вида: `https://gymflow-api.onrender.com`

### 2.5 Проверка работы backend

Открыть в браузере:

```
https://gymflow-api.onrender.com/health
```

Должен вернуться JSON:
```json
{"status":"ok"}
```

Проверить документацию API:
```
https://gymflow-api.onrender.com/docs
```

## 🎨 Шаг 3: Деплой Frontend на Netlify

### 3.1 Подключение GitHub репозитория

1. Перейти на https://netlify.com
2. Войти через GitHub
3. Нажать **"Add new site"** → **"Import an existing project"**
4. Выбрать **"Deploy with GitHub"**
5. Выбрать репозиторий `GymFlow`
6. Нажать **"Select"**

### 3.2 Настройка сборки

Заполнить форму:

| Поле | Значение |
|------|----------|
| **Branch to deploy** | `claude/backend-setup-start-011CUpeA3Yjvf432BVKuq5vi` |
| **Base directory** | `front` ⚠️ ВАЖНО! |
| **Build command** | `npm run build` |
| **Publish directory** | `front/dist` |

### 3.3 Добавление переменных окружения

В секции **Environment variables** добавить:

| Key | Value |
|-----|-------|
| `VITE_API_URL` | URL вашего backend с Render (например: `https://gymflow-api.onrender.com`) |

**ВАЖНО**: URL должен быть БЕЗ слеша в конце!

### 3.4 Запуск деплоя

1. Нажать **"Deploy site"**
2. Netlify начнёт сборку (займёт 2-3 минуты)
3. После успешного деплоя получите URL вида: `https://random-name-123.netlify.app`

### 3.5 Настройка кастомного домена (опционально)

1. Перейти в **"Site settings"** → **"Domain management"**
2. Нажать **"Add custom domain"**
3. Следовать инструкциям Netlify

## ✅ Шаг 4: Проверка работы всего стека

### 4.1 Тестирование backend

1. Откройте Swagger UI: `https://gymflow-api.onrender.com/docs`
2. Попробуйте endpoint `/sections/` - должен вернуть список секций
3. Попробуйте авторизацию: `/auth/login` с тестовыми данными:
   ```json
   {
     "login": "ivanov@mail.ru",
     "password": "user123"
   }
   ```

### 4.2 Тестирование frontend

1. Откройте ваш Netlify URL
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Каталог секций показывает данные
   - ✅ Авторизация работает
   - ✅ Регистрация работает
   - ✅ Админ-панель доступна (логин: `admin@gym.ru`, пароль: `admin123`)

### 4.3 Проверка интеграции

1. Зарегистрируйте нового пользователя через frontend
2. Проверьте, что он появился в базе данных (можно через Neon SQL Editor)
3. Создайте бронирование
4. Проверьте в админ-панели, что бронирование отображается

## 🔧 Шаг 5: Настройка мониторинга

### 5.1 Uptime мониторинг для backend

Render автоматически поддерживает `/health` endpoint с методом HEAD для uptime роботов.

Настройте мониторинг через:
- [UptimeRobot](https://uptimerobot.com) (бесплатно)
- [Pingdom](https://www.pingdom.com)
- [Better Uptime](https://betteruptime.com)

Добавьте проверку:
- **URL**: `https://gymflow-api.onrender.com/health`
- **Method**: `HEAD` или `GET`
- **Interval**: каждые 5 минут

### 5.2 Логи и отладка

**Render**:
- Перейти в Dashboard → вашсервис → **"Logs"**
- Можно фильтровать по времени

**Netlify**:
- Перейти в Site → **"Deploys"** → выбрать деплой → **"Deploy log"**

**Neon**:
- Dashboard → **"Monitoring"** для просмотра активности БД

## 🔄 Обновление приложения

### Backend (Render)

Render автоматически пересобирает при push в ветку:

```bash
git add .
git commit -m "Update backend"
git push origin claude/backend-setup-start-011CUpeA3Yjvf432BVKuq5vi
```

### Frontend (Netlify)

Netlify автоматически пересобирает при push:

```bash
git add .
git commit -m "Update frontend"
git push origin claude/backend-setup-start-011CUpeA3Yjvf432BVKuq5vi
```

## 🐛 Troubleshooting

### Backend не запускается

1. **Проверьте логи в Render**
2. **Проверьте DATABASE_URL**: Он должен начинаться с `postgresql://` (не `postgres://`)
3. **Проверьте Root Directory**: Должен быть `back`
4. **Проверьте права**: build.sh и start.sh должны быть executable

```bash
chmod +x back/build.sh back/start.sh
git add back/*.sh
git commit -m "Fix permissions"
git push
```

### Frontend не загружает данные

1. **Проверьте VITE_API_URL**: Должен быть правильный URL backend
2. **Проверьте CORS**: Backend уже настроен на `allow_origins=["*"]`
3. **Проверьте Network в DevTools**: Какие запросы идут на backend

### База данных пустая

При первом запуске backend автоматически:
1. Создаст все таблицы
2. Добавит тестовые данные

Если не создались, проверьте логи backend.

### 502 Bad Gateway на Render

Это нормально в первые 1-2 минуты после деплоя. Render запускает сервис.

Если ошибка не проходит:
1. Проверьте логи
2. Проверьте, что `start.sh` правильно запускает uvicorn

## 📊 Производительность

### Free tier ограничения

**Render Free**:
- ✅ Автоматически засыпает после 15 минут неактивности
- ⚠️ Первый запрос после сна займёт ~30 секунд
- ✅ 750 часов в месяц бесплатно

**Neon Free**:
- ✅ 0.5 GB хранилища
- ✅ 1 проект
- ✅ Автоматическое масштабирование

**Netlify Free**:
- ✅ 100 GB bandwidth/месяц
- ✅ 300 минут сборки/месяц
- ✅ Мгновенная загрузка через CDN

## 🎉 Готово!

Ваше приложение GymFlow теперь полностью задеплоено и доступно в интернете!

**Полезные ссылки**:
- 🔧 Backend API: `https://gymflow-api.onrender.com`
- 📚 API Docs: `https://gymflow-api.onrender.com/docs`
- 🎨 Frontend: `https://your-site.netlify.app`
- 🗄️ Database: Neon Dashboard

---

**Создано с помощью Claude Code** 🤖
