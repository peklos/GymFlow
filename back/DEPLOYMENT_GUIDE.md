# 🚀 GymSystem - Пошаговая инструкция по деплою

## 📋 Что нужно подготовить

- [ ] GitHub аккаунт
- [ ] Neon аккаунт (для PostgreSQL)
- [ ] Render аккаунт (для backend)
- [ ] Netlify аккаунт (для frontend - опционально)

---

## 🗂️ ШАГ 1: Подготовка проекта

### 1.1 Создайте новый репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Название: `gymsystem-backend`
4. Сделайте публичным
5. Создайте репозиторий

### 1.2 Клонируйте репозиторий локально

```bash
git clone https://github.com/your-username/gymsystem-backend.git
cd gymsystem-backend
```

### 1.3 Создайте все файлы проекта

Создайте файлы из `GYM_SYSTEM_FULL_ARCHITECTURE.md`:

**Обязательные файлы:**
```
├── .gitignore
├── requirements.txt
├── runtime.txt
├── build.sh
├── start.sh
├── main.py
├── db/
│   ├── __init__.py
│   ├── database.py
│   ├── models.py
│   └── init_data.py
├── schemas/
│   ├── __init__.py
│   ├── (все 8 schema файлов)
└── routers/
    ├── __init__.py
    └── (все 15 router файлов)
```

### 1.4 Сделайте файлы исполняемыми

```bash
chmod +x build.sh
chmod +x start.sh
```

### 1.5 Закоммитьте и запушьте

```bash
git add .
git commit -m "Initial commit: GymSystem backend"
git push origin main
```

---

## 💾 ШАГ 2: Создание базы данных на Neon

### 2.1 Регистрация

1. Зайдите на [neon.tech](https://neon.tech)
2. Нажмите "Sign up"
3. Войдите через GitHub (рекомендуется)

### 2.2 Создание проекта

1. После входа нажмите "Create a project"
2. Выберите регион: **AWS / US East (N. Virginia)**
3. Имя проекта: `gymsystem`
4. PostgreSQL версия: **15** (рекомендуется)
5. Нажмите "Create project"

### 2.3 Получение Connection String

После создания проекта вы увидите **Connection String**:

```
postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require
```

**СКОПИРУЙТЕ ЕГО!** Он понадобится для Render.

### 2.4 Проверка подключения (опционально)

Установите `psql` и проверьте подключение:

```bash
psql "postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require"
```

---

## 🌐 ШАГ 3: Деплой на Render

### 3.1 Регистрация

1. Зайдите на [render.com](https://render.com)
2. Нажмите "Get Started"
3. Войдите через GitHub

### 3.2 Создание Web Service

1. На дашборде нажмите "New +"
2. Выберите "Web Service"
3. Нажмите "Connect account" для подключения GitHub
4. Найдите репозиторий `gymsystem-backend`
5. Нажмите "Connect"

### 3.3 Настройка Web Service

**Основные настройки:**

| Поле | Значение |
|------|----------|
| **Name** | `gymsystem-api` |
| **Region** | Oregon (US West) |
| **Branch** | `main` |
| **Root Directory** | оставить пустым |
| **Runtime** | `Python 3` |
| **Build Command** | `./build.sh` |
| **Start Command** | `./start.sh` |

### 3.4 Настройка Environment Variables

Нажмите "Add Environment Variable" и добавьте:

| Key | Value |
|-----|-------|
| **DATABASE_URL** | `ваш Connection String из Neon` |
| **PYTHON_VERSION** | `3.11.0` |

**Важно:** DATABASE_URL должен быть точно таким, как из Neon!

### 3.5 Выбор плана

- Для теста выберите **Free**
- Для продакшена выберите **Starter** ($7/месяц)

### 3.6 Создание сервиса

1. Нажмите "Create Web Service"
2. Render начнет деплой (займет 3-5 минут)
3. Следите за логами во вкладке "Logs"

### 3.7 Проверка деплоя

После успешного деплоя:

1. Скопируйте URL: `https://gymsystem-api.onrender.com`
2. Откройте в браузере: `https://gymsystem-api.onrender.com/docs`
3. Вы должны увидеть Swagger UI с документацией API

---

## ✅ ШАГ 4: Проверка работы API

### 4.1 Health Check

```bash
curl https://gymsystem-api.onrender.com/health
```

Ответ:
```json
{"status": "ok"}
```

### 4.2 Проверка главной страницы

```bash
curl https://gymsystem-api.onrender.com/
```

Ответ:
```json
{
  "message": "GymSystem API работает",
  "version": "1.0.0",
  "docs": "/docs",
  ...
}
```

### 4.3 Тест регистрации

```bash
curl -X POST "https://gymsystem-api.onrender.com/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "user_data": {"login": "test@test.ru", "password": "test123"},
    "client_data": {
      "full_name": "Тест Тестов",
      "phone": "+79999999999",
      "email": "test@test.ru"
    }
  }'
```

### 4.4 Тест логина

```bash
curl -X POST "https://gymsystem-api.onrender.com/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"login": "ivanov@mail.ru", "password": "user123"}'
```

---

## 🔧 ШАГ 5: Настройка автодеплоя

### 5.1 Автоматический деплой при push

Render автоматически перезапускает сервис при push в `main` ветку.

Проверьте:
1. Внесите изменение в `README.md`
2. Закоммитьте и запушьте
3. Render автоматически начнет новый деплой

### 5.2 Настройка webhook (опционально)

В настройках Render можно настроить webhook для уведомлений о деплое в Slack/Discord.

---

## 🌐 ШАГ 6: Подключение frontend (Netlify)

### 6.1 Создание frontend проекта

Создайте отдельный репозиторий `gymsystem-frontend` с React/Vue/Next.js

### 6.2 Настройка API URL

В frontend проекте создайте `.env`:

```env
VITE_API_URL=https://gymsystem-api.onrender.com
# или для Next.js:
NEXT_PUBLIC_API_URL=https://gymsystem-api.onrender.com
```

### 6.3 Деплой на Netlify

1. Зайдите на [netlify.com](https://www.netlify.com)
2. Войдите через GitHub
3. Нажмите "Add new site" → "Import an existing project"
4. Выберите репозиторий `gymsystem-frontend`
5. Build settings:
   - **Build command:** `npm run build` (или `yarn build`)
   - **Publish directory:** `dist` (или `build`)
6. Environment variables: добавьте `VITE_API_URL`
7. Нажмите "Deploy site"

---

## 📊 ШАГ 7: Мониторинг

### 7.1 Логи в Render

Во вкладке "Logs" вы можете видеть:
- Запросы к API
- Ошибки
- SQL запросы (если включить в database.py `echo=True`)

### 7.2 Метрики в Render

Во вкладке "Metrics" доступны:
- CPU usage
- Memory usage
- Request count
- Response time

### 7.3 Логи в Neon

В дашборде Neon можно видеть:
- Количество подключений
- Размер базы данных
- Активные запросы

---

## 🐛 Решение проблем

### Проблема 1: Build failed

**Причина:** Ошибка в `requirements.txt` или `build.sh`

**Решение:**
1. Проверьте логи Render
2. Убедитесь, что `build.sh` имеет права на выполнение
3. Проверьте, что все зависимости в `requirements.txt`

### Проблема 2: Database connection error

**Причина:** Неверный `DATABASE_URL`

**Решение:**
1. Проверьте `DATABASE_URL` в Render environment variables
2. Убедитесь, что нет пробелов в начале/конце
3. Проверьте, что используется `postgresql://` а не `postgres://`
4. В `database.py` должна быть замена `postgres://` на `postgresql://`

### Проблема 3: Application failed to respond

**Причина:** Неверный `start.sh` или порт

**Решение:**
1. Убедитесь, что в `start.sh` используется `${PORT:-8000}`
2. Render автоматически предоставляет переменную `PORT`
3. Проверьте, что `start.sh` имеет права на выполнение

### Проблема 4: Free tier sleep

**Причина:** Render free tier засыпает после 15 минут неактивности

**Решение:**
1. Первый запрос после сна займет 30-60 секунд
2. Для production используйте платный план
3. Можно настроить cron-задачу для пинга API каждые 10 минут

---

## 🔄 Обновление проекта

### Обновление кода

```bash
# Внесите изменения в код
git add .
git commit -m "Update: описание изменений"
git push origin main

# Render автоматически задеплоит изменения
```

### Обновление зависимостей

```bash
# Обновите requirements.txt
pip freeze > requirements.txt

# Закоммитьте и запушьте
git add requirements.txt
git commit -m "Update dependencies"
git push origin main
```

### Миграции базы данных

Если нужно изменить структуру БД:

1. Обновите модели в `db/models.py`
2. SQLAlchemy автоматически создаст новые таблицы при запуске
3. Для изменения существующих таблиц используйте Alembic (см. документацию)

---

## 📝 Чеклист перед продакшеном

- [ ] Все тесты проходят
- [ ] CORS настроен правильно (только нужные домены)
- [ ] Логирование настроено
- [ ] Backup базы данных настроен (Neon auto-backup)
- [ ] Мониторинг ошибок (Sentry)
- [ ] SSL сертификат активен (Render предоставляет автоматически)
- [ ] Документация API обновлена
- [ ] Environment variables проверены
- [ ] Rate limiting настроен (опционально)
- [ ] Health check работает

---

## 🎯 Результат

После выполнения всех шагов у вас будет:

✅ Backend API на Render  
✅ PostgreSQL база на Neon  
✅ Автоматический деплой при push  
✅ Swagger документация доступна публично  
✅ Готово для подключения frontend  

**URL вашего API:**
```
https://gymsystem-api.onrender.com
```

**Документация:**
```
https://gymsystem-api.onrender.com/docs
```

---

## 📞 Поддержка

- **Render Docs:** https://render.com/docs
- **Neon Docs:** https://neon.tech/docs
- **FastAPI Docs:** https://fastapi.tiangolo.com

---

**Поздравляем с успешным деплоем! 🎉🚀**
